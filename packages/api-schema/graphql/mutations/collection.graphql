mutation attachContentToCollection($collectionId: Int!, $contentIds: [Int!]!, $insertOrder: Int) {
  attachContentsToCollection(
    collectionId: $collectionId
    contentIds: $contentIds
    insertOrder: $insertOrder
  )
}
